syntax = "proto3";

package app.player;

option java_multiple_files = true;
option java_package = "io.renagc.awanotsu.proto.player";
option java_outer_classname = "PlayerServiceProto";

import "app/common.proto";

// Auto-generated by tools/apply_messages.py from the recovered
// il2cpp dump. RPC set authoritative from __Marshaller_app_* symbols;
// message field bodies recovered from the Google.Protobuf C# classes.

service PlayerService {
  rpc ChangeFavoriteMember(ChangeFavoriteMemberRequest) returns (ChangeFavoriteMemberResponse);
  rpc CheckNgWord(CheckNgWordRequest) returns (CheckNgWordResponse);
  rpc EditProfile(EditProfileRequest) returns (EditProfileResponse);
  rpc GetConnectedPlayer(GetConnectedPlayerRequest) returns (GetConnectedPlayerResponse);
  rpc GetPlayerData(GetPlayerDataRequest) returns (GetPlayerDataResponse);
  rpc Register(RegisterRequest) returns (RegisterResponse);
  rpc RegisterConnection(RegisterConnectionRequest) returns (RegisterConnectionResponse);
  rpc RegisterDevice(RegisterDeviceRequest) returns (RegisterDeviceResponse);
  rpc RegisterDeviceForConnectedPlayer(RegisterDeviceForConnectedPlayerRequest) returns (RegisterDeviceForConnectedPlayerResponse);
  rpc RemovePlayerData(RemovePlayerDataRequest) returns (RemovePlayerDataResponse);
  rpc Whoami(WhoamiRequest) returns (WhoamiResponse);
}

message ChangeFavoriteMemberRequest {
  int64 member_id = 1;
}

message ChangeFavoriteMemberResponse {
  // (no fields in dump)
}

message CheckNgWordRequest {
  string word = 1;
}

message CheckNgWordResponse {
  // (no fields in dump)
}

message EditProfileRequest {
  string name = 1;
}

message EditProfileResponse {
  // (no fields in dump)
}

message GetConnectedPlayerRequest {
  string player_id = 1;
  string password = 2;
}

message GetConnectedPlayerResponse {
  string nickname = 1;
  int32 player_rank = 2;
}

message GetPlayerDataRequest {
  // (no fields in dump)
}

message GetPlayerDataResponse {
  app.common.PlayerData player_data = 1;
  app.common.Notification notification = 2;
  app.common.Limitation limitation = 3;
  app.common.Friends friends = 4;
  app.common.Invitation invitation = 5;
}

message RegisterRequest {
  string initial_data_group = 1;
}

message RegisterResponse {
  app.common.PlayerCredential credential = 1;
  int64 profile_id = 2;
}

message RegisterConnectionRequest {
  string password = 1;
}

message RegisterConnectionResponse {
  // (no fields in dump)
}

message RegisterDeviceRequest {
  // (no fields in dump)
}

message RegisterDeviceResponse {
  app.common.PlayerCredential credential = 1;
}

message RegisterDeviceForConnectedPlayerRequest {
  string player_id = 1;
  string password = 2;
}

message RegisterDeviceForConnectedPlayerResponse {
  app.common.PlayerCredential credential = 1;
}

message RemovePlayerDataRequest {
  // (no fields in dump)
}

message RemovePlayerDataResponse {
  // (no fields in dump)
}

message WhoamiRequest {
  // (no fields in dump)
}

message WhoamiResponse {
  string player_id = 1;
}
