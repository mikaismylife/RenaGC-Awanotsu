syntax = "proto3";

package app.present;

option java_multiple_files = true;
option java_package = "io.renagc.awanotsu.proto.present";
option java_outer_classname = "PresentServiceProto";

import "app/common.proto";

// Auto-generated by tools/apply_messages.py from the recovered
// il2cpp dump. RPC set authoritative from __Marshaller_app_* symbols;
// message field bodies recovered from the Google.Protobuf C# classes.

service PresentService {
  rpc Fetch(FetchRequest) returns (FetchResponse);
  rpc History(HistoryRequest) returns (HistoryResponse);
  rpc Open(OpenRequest) returns (OpenResponse);
}

message FetchRequest {
  // (no fields in dump)
}

message FetchResponse {
  repeated app.common.Present presents = 1;
}

message HistoryRequest {
  // (no fields in dump)
}

message HistoryResponse {
  repeated app.common.PresentHistory histories = 1;
}

message OpenRequest {
  repeated uint64 present_ids = 1;
}

message OpenResponse {
  repeated app.common.PresentUnreceived unreceived = 1;
}
