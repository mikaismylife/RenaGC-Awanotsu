syntax = "proto3";

package app.shop;

option java_multiple_files = true;
option java_package = "io.renagc.awanotsu.proto.shop";
option java_outer_classname = "ShopServiceProto";

import "app/common.proto";
import "google/protobuf/empty.proto";

// Auto-generated by tools/apply_messages.py from the recovered
// il2cpp dump. RPC set authoritative from __Marshaller_app_* symbols;
// message field bodies recovered from the Google.Protobuf C# classes.

service ShopService {
  rpc CheckMaintenanceShop(CheckMaintenanceShopRequest) returns (CheckMaintenanceShopResponse);
  rpc Fetch(FetchRequest) returns (FetchResponse);
  rpc History(HistoryRequest) returns (HistoryResponse);
  rpc Purchase(PurchaseRequest) returns (PurchaseResponse);
  rpc PurchaseCash(PurchaseCashRequest) returns (google.protobuf.Empty);
  rpc PurchaseCM(PurchaseCMRequest) returns (google.protobuf.Empty);
  rpc SaveBirth(SaveBirthRequest) returns (SaveBirthResponse);
}

message CheckMaintenanceShopRequest {
  // (no fields in dump)
}

message CheckMaintenanceShopResponse {
  // (no fields in dump)
}

message FetchRequest {
  // (no fields in dump)
}

message FetchResponse {
  repeated app.common.ShopProduct products = 1;
  int32 year = 2;
  int32 month = 3;
  int32 monthly_total_payment = 4;
}

message HistoryRequest {
  // (no fields in dump)
}

message HistoryResponse {
  repeated app.common.ShopPurchaseHistory histories = 1;
}

message PurchaseRequest {
  int64 shop_id = 1;
  int32 purchase_count = 2;
}

message PurchaseResponse {
  // (no fields in dump)
}

message PurchaseCashRequest {
  int64 shop_id = 1;
  string signature = 2;
  string receipt = 3;
}

message PurchaseCMRequest {
  int64 shop_id = 1;
}

message SaveBirthRequest {
  int32 year = 1;
  int32 month = 2;
}

message SaveBirthResponse {
  // (no fields in dump)
}
