syntax = "proto3";

package app.livemusic;

option java_multiple_files = true;
option java_package = "io.renagc.awanotsu.proto.livemusic";
option java_outer_classname = "LiveMusicServiceProto";

import "app/common.proto";

// Auto-generated by tools/apply_messages.py from the recovered
// il2cpp dump. RPC set authoritative from __Marshaller_app_* symbols;
// message field bodies recovered from the Google.Protobuf C# classes.

service LiveMusicService {
  rpc GetRanking(GetRankingRequest) returns (GetRankingResponse);
  rpc SaveFavorite(SaveFavoriteRequest) returns (SaveFavoriteResponse);
}

message GetRankingRequest {
  int64 music_id = 1;
}

message GetRankingResponse {
  repeated app.common.LiveRankingPlayer players = 1;
  int32 my_rank = 2;
}

message SaveFavoriteRequest {
  int64 live_music_master_id = 1;
  bool is_favorite_1 = 2;
  bool is_favorite_2 = 3;
  bool is_favorite_3 = 4;
}

message SaveFavoriteResponse {
  // (no fields in dump)
}
