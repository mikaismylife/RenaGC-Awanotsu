syntax = "proto3";

package app.gacha;

option java_multiple_files = true;
option java_package = "io.renagc.awanotsu.proto.gacha";
option java_outer_classname = "GachaServiceProto";

import "app/common.proto";

// Auto-generated by tools/apply_messages.py from the recovered
// il2cpp dump. RPC set authoritative from __Marshaller_app_* symbols;
// message field bodies recovered from the Google.Protobuf C# classes.

service GachaService {
  rpc CheckMaintenanceGacha(CheckMaintenanceGachaRequest) returns (CheckMaintenanceGachaResponse);
  rpc ConvertPoint(ConvertPointRequest) returns (ConvertPointResponse);
  rpc Execute(ExecuteRequest) returns (ExecuteResponse);
  rpc History(HistoryRequest) returns (HistoryResponse);
}

message CheckMaintenanceGachaRequest {
  // (no fields in dump)
}

message CheckMaintenanceGachaResponse {
  // (no fields in dump)
}

message ConvertPointRequest {
  repeated int64 exchange_id = 1;
}

message ConvertPointResponse {
  app.common.Notification notification = 1;
}

message ExecuteRequest {
  int64 gacha_id = 1;
  int64 product_id = 2;
  repeated int64 selected_pick_up = 3;
}

message ExecuteResponse {
  repeated int64 prize_id = 1;
  repeated int64 bonus_lot_id = 2;
  app.common.Notification notification = 3;
}

message HistoryRequest {
  // (no fields in dump)
}

message HistoryResponse {
  repeated app.common.GachaHistory history = 1;
  repeated app.common.GachaExecutionHistory execution_history = 2;
}
